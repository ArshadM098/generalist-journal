export default function SocialButton({icon}){
    return(
        <div className={` hover:first:bg-green-500`}>
            <div className={`border-[1px] border-solid border-slate-500 text-opacity-0 ${color}`}>{children}</div>
            <div className="border-[1px] border-solid border-slate-500">{children}</div>
        </div>)
        }